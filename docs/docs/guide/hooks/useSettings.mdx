# useSettings

## Overview

The `useSettings()` hook is a React hook that allows you to access the settings of your app. This hook enables you to modify the settings of your app and also update distinct settings to different pages within it.

## Values

The `useSettings()` hook provides the following variables, allowing you to customize the settings of your app.

| Value             | Type                                                                     | Description                                                 |
| ----------------- | ------------------------------------------------------------------------ | ----------------------------------------------------------- |
| settings          | `Settings`                                                               | The settings of your app.                                   |
| isSettingsChanged | `boolean`                                                                | Indicates whether the initial settings changed.             |
| updateSettings    | `(settings: Partial<Settings>, options?: UpdateSettingsOptions) => void` | A function that updates the settings of your app.           |
| resetSettings     | `() => void`                                                             | A function that reset the settings to the initial settings. |

```ts noCopy
export type Settings = {
  mode?: Mode
  skin?: Skin
  semiDark?: boolean
  layout?: Layout
  navbarContentWidth?: LayoutComponentWidth
  contentWidth?: LayoutComponentWidth
  footerContentWidth?: LayoutComponentWidth
  primaryColor?: string
}
```

```ts noCopy
type UpdateSettingsOptions = {
  updateCookie?: boolean
}
```

The `updateCookie` prop allows you to update the settings of your app in the browser's cookies. By default, this prop is set to `true`.

## Usage

To utilize the `useSettings()` hook, please refer to the following examples:

import TsToJsCodeSnippet from "@docComponents/ts-js-code"
import IframeWindow from "@docComponents/browser-window/IframeWindow.tsx"

### Mode

import SourceCodeMode from "!!raw-loader!@/app/[lang]/hook-examples/use-settings/settings-mode/page"

<IframeWindow url="http://localhost:3000/en/hook-examples/use-settings/settings-mode" />

#### Source Code

<TsToJsCodeSnippet>{SourceCodeMode}</TsToJsCodeSnippet>

### Skin

import SourceCodeSkin from "!!raw-loader!@/app/[lang]/hook-examples/use-settings/settings-skin/page"

<IframeWindow url="http://localhost:3000/en/hook-examples/use-settings/settings-skin" />

#### Source Code

<TsToJsCodeSnippet>{SourceCodeSkin}</TsToJsCodeSnippet>

### SemiDark

import SourceCodeSemiDark from "!!raw-loader!@/app/[lang]/hook-examples/use-settings/settings-semi-dark/page"

<IframeWindow url="http://localhost:3000/en/hook-examples/use-settings/settings-semi-dark" />

#### Source Code

<TsToJsCodeSnippet>{SourceCodeSemiDark}</TsToJsCodeSnippet>

### Layout

import SourceCodeLayout from "!!raw-loader!@/app/[lang]/hook-examples/use-settings/settings-layout/page"

<IframeWindow url="http://localhost:3000/en/hook-examples/use-settings/settings-layout" />

#### Source Code

<TsToJsCodeSnippet>{SourceCodeLayout}</TsToJsCodeSnippet>

### navbarContentWidth

import SourceCodeNavbarContentWidth from "!!raw-loader!@/app/[lang]/hook-examples/use-settings/settings-navbar-content-width/page"

<IframeWindow url="http://localhost:3000/en/hook-examples/use-settings/settings-navbar-content-width" />

#### Source Code

<TsToJsCodeSnippet>{SourceCodeNavbarContentWidth}</TsToJsCodeSnippet>

### ContentWidth

import SourceCodeContentWidth from "!!raw-loader!@/app/[lang]/hook-examples/use-settings/settings-content-width/page"

<IframeWindow url="http://localhost:3000/en/hook-examples/use-settings/settings-content-width" />

#### Source Code

<TsToJsCodeSnippet>{SourceCodeContentWidth}</TsToJsCodeSnippet>

### footerContentWidth

import SourceCodeFooterContentWidth from "!!raw-loader!@/app/[lang]/hook-examples/use-settings/settings-footer-content-width/page"

<IframeWindow url="http://localhost:3000/en/hook-examples/use-settings/settings-footer-content-width" />

#### Source Code

<TsToJsCodeSnippet>{SourceCodeFooterContentWidth}</TsToJsCodeSnippet>

### primaryColor

import SourceCodePrimaryColor from "!!raw-loader!@/app/[lang]/hook-examples/use-settings/settings-primary-color/page";

<IframeWindow url="http://localhost:3000/en/hook-examples/use-settings/settings-primary-color" />

#### Source Code

<TsToJsCodeSnippet>{SourceCodePrimaryColor}</TsToJsCodeSnippet>

### Change/Reset SettingsMode

import SourceCodeChangeSettingsMode from "!!raw-loader!@/app/[lang]/hook-examples/use-settings/settings-changed/page"

<IframeWindow url="http://localhost:3000/en/hook-examples/use-settings/settings-changed" />

#### Source Code

<TsToJsCodeSnippet>{SourceCodeChangeSettingsMode}</TsToJsCodeSnippet>
