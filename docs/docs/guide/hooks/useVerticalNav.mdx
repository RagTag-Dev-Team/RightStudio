# useVerticalNav

## Overview

The `useVerticalNav()` hook is a custom hook specifically designed to handle the functionality and state management of a navigation sidebar within a web application. Its purpose is to provide a comprehensive set of variables that can be utilized to customize the behavior of the navigation sidebar. This documentation has explained how to use the hook and described variables for configuring and customizing the menu.

## Values

The `useVerticalNav()` hook provides the following variables that can be used to customize the behavior and appearance of the navigation sidebar:

| Value                  | Type                                                | Description                                                                                      |
| ---------------------- | --------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| width                  | `number`                                            | Determines the width of the navigation sidebar.                                                  |
| collapsedWidth         | `number`                                            | Specifies the width of the navigation sidebar when it is collapsed.                              |
| isCollapsed            | `boolean`                                           | Indicates whether the navigation sidebar is currently collapsed.                                 |
| isHovered              | `boolean`                                           | Indicates whether the navigation sidebar is currently being hovered over when menu is collapsed. |
| isToggled              | `boolean`                                           | Indicates whether the navigation sidebar is currently toggled.                                   |
| isScrollWithContent    | `boolean`                                           | Determines whether the navigation sidebar scrolls with the content.                              |
| isPopoutWhenCollapsed  | `boolean`                                           | Determines whether the navigation sidebar pops out when it is collapsed.                         |
| isBreakpointReached    | `boolean`                                           | Indicates whether the specified breakpoint has been reached.                                     |
| transitionDuration     | `number`                                            | Specifies the duration (in `ms`) of the transition for the entire navigation menu.               |
| updateVerticalNavState | `(values: VerticalNavState) => void`                | A function used to update the state of the navigation sidebar.                                   |
| collapseVerticalNav    | `(value?: VerticalNavState['isCollapsed']) => void` | A function used to collapse the navigation sidebar.                                              |
| hoverVerticalNav       | `(value?: VerticalNavState['isHovered']) => void`   | A function used to apply a hover effect to the navigation sidebar.                               |
| toggleVerticalNav      | `(value?: VerticalNavState['isToggled']) => void`   | A function used to toggle the visibility of the navigation sidebar.                              |

## Usage

To utilize the `useVerticalNav()` hook, please refer to the following examples:

import BrowserWindow from "@docComponents/browser-window";
import Provider from "@docComponents/Providers";
import CollapsedFunction from "@docPages/hook-examples/use-vertical-nav/CollapsedFunction";
import HoverFunction from "@docPages/hook-examples/use-vertical-nav/HoverFunction";
import IsBreakpointReached from "@docPages/hook-examples/use-vertical-nav/IsBreakpointReached";
import IsCollapsed from "@docPages/hook-examples/use-vertical-nav/IsCollapsed";
import IsHovered from "@docPages/hook-examples/use-vertical-nav/IsHovered";
import IsPopoutWhenCollapsed from "@docPages/hook-examples/use-vertical-nav/IsPopoutWhenCollapsed";
import IsScrollWithContent from "@docPages/hook-examples/use-vertical-nav/IsScrollWithContent";
import IsToggled from "@docPages/hook-examples/use-vertical-nav/IsToggled";
import ToggleFunction from "@docPages/hook-examples/use-vertical-nav/ToggleFunction";
import TsToJsCodeSnippet from "@docComponents/ts-js-code";

### toggleVerticalNav

import SourceCodeToggleFunction from "!!raw-loader!@docPages/hook-examples/use-vertical-nav/ToggleFunction";

export const updatedSourceCodeToggleFunction = SourceCodeToggleFunction.replace(
  /customStyles=\{\{([\s\S]*?)\}\}/,
  ""
)
  .replace(/backgroundColor='.*'/, "")
  .replace(/\s*\/\/\sStyle.*\s*import menuItemStyles.*/, "")
  .replace(/menuItemStyles={.*}/, "");

<BrowserWindow url="/use-vertical-nav/toggle-function">
  <Provider>
    <ToggleFunction />
  </Provider>
</BrowserWindow>

#### Source Code

<TsToJsCodeSnippet>{updatedSourceCodeToggleFunction}</TsToJsCodeSnippet>

### isToggled

import SourceCodeIsToggled from "!!raw-loader!@docPages/hook-examples/use-vertical-nav/ToggleFunction";

export const updatedSourceCodeIsToggled = SourceCodeIsToggled.replace(
  /customStyles=\{\{([\s\S]*?)\}\}/,
  ""
)
  .replace("flex min-bs-full bs-dvh", "flex")
  .replace(/backgroundColor='.*'/, "")
  .replace(/\s*\/\/\sStyle.*\s*import menuItemStyles.*/, "")
  .replace(/menuItemStyles={.*}/, "");

<BrowserWindow url="/use-vertical-nav/is-toggled">
  <Provider>
    <IsToggled />
  </Provider>
</BrowserWindow>

#### Source Code

<TsToJsCodeSnippet>{updatedSourceCodeIsToggled}</TsToJsCodeSnippet>

### collapseVerticalNav

import SourceCodeCollapsedFunction from "!!raw-loader!@docPages/hook-examples/use-vertical-nav/CollapsedFunction";

export const updatedSourceCodeCollapsedFunction =
  SourceCodeCollapsedFunction.replace(/customStyles=\{\{([\s\S]*?)\}\}/, "")
    .replace("flex min-bs-full bs-dvh", "flex")
    .replace(/backgroundColor='.*'/, "")
    .replace(/\s*\/\/\sStyle.*\s*import menuItemStyles.*/, "")
    .replace(/menuItemStyles={.*}/, "");

<BrowserWindow url="/use-vertical-nav/collapsed-function">
  <Provider>
    <CollapsedFunction />
  </Provider>
</BrowserWindow>

#### Source Code

<TsToJsCodeSnippet>{updatedSourceCodeCollapsedFunction}</TsToJsCodeSnippet>

### isCollapsed

import SourceCodeIsCollapsed from "!!raw-loader!@docPages/hook-examples/use-vertical-nav/IsCollapsed";

export const updatedSourceCodeIsCollapsed = SourceCodeIsCollapsed.replace(
  /customStyles=\{\{([\s\S]*?)\}\}/,
  ""
)
  .replace("flex min-bs-full bs-dvh", "flex")
  .replace(/backgroundColor='.*'/, "")
  .replace(/\s*\/\/\sStyle.*\s*import menuItemStyles.*/, "")
  .replace(/menuItemStyles={.*}/, "");

<BrowserWindow url="/use-vertical-nav/is-collapsed">
  <Provider>
    <IsCollapsed />
  </Provider>
</BrowserWindow>

#### Source Code

<TsToJsCodeSnippet>{updatedSourceCodeIsCollapsed}</TsToJsCodeSnippet>

### hoverVerticalNav

import SourceCodeHoverFunction from "!!raw-loader!@docPages/hook-examples/use-vertical-nav/HoverFunction";

export const updatedSourceCodeHoverFunction = SourceCodeHoverFunction.replace(
  /customStyles=\{\{([\s\S]*?)\}\}/,
  ""
)
  .replace("flex min-bs-full bs-dvh", "flex")
  .replace(/backgroundColor='.*'/, "")
  .replace(/\s*\/\/\sStyle.*\s*import menuItemStyles.*/, "")
  .replace(/menuItemStyles={.*}/, "");

<BrowserWindow url="/use-vertical-nav/hover-function">
  <Provider>
    <HoverFunction />
  </Provider>
</BrowserWindow>

#### Source Code

<TsToJsCodeSnippet>{updatedSourceCodeHoverFunction}</TsToJsCodeSnippet>

### isHovered

import SourceCodeIsHovered from "!!raw-loader!@docPages/hook-examples/use-vertical-nav/IsHovered";

export const updatedSourceCodeIsHovered = SourceCodeIsHovered.replace(
  /customStyles=\{\{([\s\S]*?)\}\}/,
  ""
)
  .replace("flex min-bs-full bs-dvh", "flex")
  .replace(/backgroundColor='.*'/, "")
  .replace(/\s*\/\/\sStyle.*\s*import menuItemStyles.*/, "")
  .replace(/menuItemStyles={.*}/, "");

<BrowserWindow url="/use-vertical-nav/is-hovered">
  <Provider>
    <IsHovered />
  </Provider>
</BrowserWindow>

#### Source Code

<TsToJsCodeSnippet>{updatedSourceCodeIsHovered}</TsToJsCodeSnippet>

### isScrollWithContent

import SourceCodeIsScrollWithContent from "!!raw-loader!@docPages/hook-examples/use-vertical-nav/IsScrollWithContent";

export const updatedSourceCodeIsScrollWithContent =
  SourceCodeIsScrollWithContent.replace(/customStyles=\{\{([\s\S]*?)\}\}/, "")
    .replace("flex min-bs-full bs-dvh overflow-auto", "flex")
    .replace(/backgroundColor='.*'/, "")
    .replace(/\s*\/\/\sStyle.*\s*import menuItemStyles.*/, "")
    .replace(/menuItemStyles={.*}/, "");

<BrowserWindow url="/use-vertical-nav/is-scroll-with-content">
  <Provider>
    <IsScrollWithContent />
  </Provider>
</BrowserWindow>

#### Source Code

<TsToJsCodeSnippet>{updatedSourceCodeIsScrollWithContent}</TsToJsCodeSnippet>

### isPopoutWhenCollapsed

import SourceCodeIsPopoutWhenCollapsed from "!!raw-loader!@docPages/hook-examples/use-vertical-nav/IsPopoutWhenCollapsed";

export const updatedSourceCodeIsPopoutWhenCollapsed =
  SourceCodeIsPopoutWhenCollapsed.replace(/customStyles=\{\{([\s\S]*?)\}\}/, "")
    .replace("flex min-bs-full bs-dvh", "flex")
    .replace(/backgroundColor='.*'/, "")
    .replace(/\s*\/\/\sStyle.*\s*import menuItemStyles.*/, "")
    .replace(/menuItemStyles={.*}/, "");

<BrowserWindow url="/use-vertical-nav/is-popout-when-collapsed">
  <Provider>
    <IsPopoutWhenCollapsed />
  </Provider>
</BrowserWindow>

#### Source Code

<TsToJsCodeSnippet>{updatedSourceCodeIsPopoutWhenCollapsed}</TsToJsCodeSnippet>

### isBreakpointReached

import SourceCodeIsBreakpointReached from "!!raw-loader!@docPages/hook-examples/use-vertical-nav/IsBreakpointReached";

export const updatedSourceCodeIsBreakpointReached =
  SourceCodeIsBreakpointReached.replace(/customStyles=\{\{([\s\S]*?)\}\}/, "")
    .replace("flex min-bs-full bs-dvh", "flex")
    .replace(/backgroundColor='.*'/, "")
    .replace(/\s*\/\/\sStyle.*\s*import menuItemStyles.*/, "")
    .replace(/menuItemStyles={.*}/, "");

<BrowserWindow url="/use-vertical-nav/is-breakpoint-reached">
  <Provider>
    <IsBreakpointReached />
  </Provider>
</BrowserWindow>

#### Source Code

<TsToJsCodeSnippet>{updatedSourceCodeIsBreakpointReached}</TsToJsCodeSnippet>
