# useMediaQuery

## Overview

The `useMediaQuery()` is a custom hook used to determine if the current viewport matches a given media query.

### Usages

To utilize the `useMediaQuery()` hook, please refer to the example provided below:

import BrowserWindow from "@docComponents/browser-window";
import Provider from "@docComponents/Providers";
import MediaQuery from "@docPages/hook-examples/use-media-query";
import TsToJsCodeSnippet from "@docComponents/ts-js-code";
import SourceCodeMediaQuery from "!raw-loader!@docPages/hook-examples/use-media-query/index";

### Example

<BrowserWindow url="/use-media-query">
  <Provider>
    <MediaQuery />
  </Provider>
</BrowserWindow>

#### Source Code

<TsToJsCodeSnippet>{SourceCodeMediaQuery}</TsToJsCodeSnippet>
