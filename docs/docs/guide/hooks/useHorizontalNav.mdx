import BrowserWindow from "@docComponents/browser-window";
import Provider from "@docComponents/Providers";
import IsBreakpointReached from "@docPages/hook-examples/use-horizontal-nav/IsBreakpointReached";
import UpdateIsBreakpointReached from "@docPages/hook-examples/use-horizontal-nav/UpdateIsBreakpointReached";
import TsToJsCodeSnippet from "@docComponents/ts-js-code";
import SourceCodeIsBreakpointReached from "!raw-loader!@docPages/hook-examples/use-horizontal-nav/IsBreakpointReached";
import SourceCodeUpdateIsBreakpointReached from "!raw-loader!@docPages/hook-examples/use-horizontal-nav/UpdateIsBreakpointReached";

# useHorizontalNav

## Overview

The `useHorizontalNav()` hook is a custom hook specifically designed to handle the functionality of a navigation menu within a web application. Its purpose is to provide a comprehensive variable that can be utilized to customize the behavior of the navigation menu. This documentation has explained how to use the hook and described variables for configuring and customizing the navigation menu.

## Values

The `useHorizontalNav()` hook provides the following variable that can be used to customize the behavior of the navigation menu:

| Value                     | Type                                     | Description                                                             |
| ------------------------- | ---------------------------------------- | ----------------------------------------------------------------------- |
| isBreakpointReached       | `boolean`                                | A boolean value that indicates whether the breakpoint has been reached. |
| updateIsBreakpointReached | `(isBreakpointReached: boolean) => void` | A function used to update isBreakpointReached value.                    |

## Usage

To utilize the `useHorizontalNav()` hook, please refer to the example provided below:

### isBreakpointReached

export const updatedSourceCodeIsBreakpointReached =
  SourceCodeIsBreakpointReached.replace(
    /\s*\/\/.*\s*\/\/.*\s*verticalNavProps=\{\{([\s\S]*?)\}\}/,
    ""
  )
    .replace(/backgroundColor='.*'/, "")
    .replace(/\s*\/\/\sStyle.*\s*import menuItemStyles.*/, "")
    .replace(/menuItemStyles={.*}/, "");

<BrowserWindow url="/use-horizontal-nav/is-breakpoint-reached">
  <Provider>
    <IsBreakpointReached />
  </Provider>
</BrowserWindow>

#### Source Code

<TsToJsCodeSnippet>{updatedSourceCodeIsBreakpointReached}</TsToJsCodeSnippet>

### updateIsBreakpointReached

<BrowserWindow url="/use-horizontal-nav/updated-is-breakpoint-reached">
  <Provider>
    <UpdateIsBreakpointReached />
  </Provider>
</BrowserWindow>

#### Source Code

<TsToJsCodeSnippet>{SourceCodeUpdateIsBreakpointReached}</TsToJsCodeSnippet>
