---
hide_table_of_contents: true
---

import BrowserWindow from '@docComponents/browser-window'
import Provider from '@docComponents/Providers'
import StaticMenu from '@docPages/menu-examples/vertical-menu/menu-render/StaticMenu'
import TsToJsCodeSnippet from '@docComponents/ts-js-code'
import SourceCode from '!!raw-loader!@docPages/menu-examples/vertical-menu/menu-render/StaticMenu'

# Static Menu

The static menu is the simplest way to create a menu where you render the menu components directly in your code without any dynamic data source. It is useful when you have a fixed set of menu items that do not change based on external factors.

### Example

<BrowserWindow>
  <Provider>
    <StaticMenu />
  </Provider>
</BrowserWindow>

### Source Code

<TsToJsCodeSnippet>{SourceCode}</TsToJsCodeSnippet>
