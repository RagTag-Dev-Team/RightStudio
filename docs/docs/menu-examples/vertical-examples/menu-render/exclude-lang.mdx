---
hide_table_of_contents: true
---

import BrowserWindow from '@docComponents/browser-window'
import Provider from '@docComponents/Providers'
import ExcludeLang from '@docPages/menu-examples/vertical-menu/menu-render/ExcludeLang'
import TsToJsCodeSnippet from '@docComponents/ts-js-code'
import SourceCode from '!!raw-loader!@docPages/menu-examples/vertical-menu/menu-render/ExcludeLang'

# Exclude Lang

export const updatedCode = SourceCode.replace(/customStyles=\{\{([\s\S]*?)\}\}/, '').replace('@docComponents/GenerateMenu', '@compnoents/GenerateMenu').replace('@site/src/types/menuTypes', '@/types/menuTypes').replace(/backgroundColor='.*'/, '').replace(/\s*\/\/\sStyle.*\s*import menuItemStyles.*/, '').replace(/\s*\/\/\sMUI.*\s*import\s{ deepmerge.*/,'').replace(/menuItemStyles={.*}/, '').replace(/(label: 'Analytics')/, "$1,href: '/dashboards/analytics'").replace(/(label: 'eCommerce')/, "$1,href: '/dashboards/ecommerce'").replace(/(label: 'Front Pages')/, "$1,href: '/front-pages/landing-page', target='_blank'")

The `excludeLang` prop allows you to exclude the localization of URLs in the menu. This is useful when you want to render menu items with URLs that are not localized. The `excludeLang` prop can only be used with dynamic menus.

If you are using i18n in your project, your app's pages typically reside inside the `src/app/[lang]` folder. However, you may also have some pages outside the `src/app/[lang]` folder where you don't want localization. To prevent localization of these URLs, simply add `excludeLang: true` to the respective menu items. By default, `excludeLang` is set to `false`, so URLs are localized unless you specify `excludeLang: true`.

We have implemented this prop in the `verticalMenuData.tsx` file located in the `src/data/navigation` folder.

### Props

```ts noCopy
excludeLang?: boolean
```

```ts noCopy title='Default Value'
excludeLang={false}
```

### Example

Here we have used dynamic menu example to render the vertical menu. You may refer to the [Dynamic Menu](/docs/menu-examples/vertical-examples/menu-render/dynamic-menu) example for more information on the dynamic menu rendering.

<BrowserWindow url='/vertical-menu/menu-render/exclude-lang'>
  <Provider>
    <ExcludeLang />
  </Provider>
</BrowserWindow>

### Source Code

<TsToJsCodeSnippet>{updatedCode}</TsToJsCodeSnippet>
