---
hide_table_of_contents: true
---

# Nav Collpase Icons

import BrowserWindow from '@docComponents/browser-window'
import Provider from '@docComponents/Providers'
import NavCollapseIcons from '@docPages/menu-examples/vertical-menu/NavCollapseIcons'
import TsToJsCodeSnippet from '@docComponents/ts-js-code'
import SourceCode from '!!raw-loader!@docPages/menu-examples/vertical-menu/NavCollapseIcons'

import VerticalNavCollapseIconsProps from '@site/src/components/props/vertical-props/VerticalNavCollapseIcons'

export const updatedCode = SourceCode.replace(/customStyles=\{\{([\s\S]*?)\}\}/, '').replace('flex min-bs-full bs-dvh', 'flex').replace(/backgroundColor='.*'/, '').replace(/\s*\/\/\sStyle.*\s*import menuItemStyles.*/, '').replace(/menuItemStyles={.*}/, '')

## Props with types

<table>
  <thead>
    <tr>
      <th>Props</th>
      <th>Type</th>
      <th>Description</th>
      <th>Default</th>
    </tr>
  </thead>
  <tbody>
    <VerticalNavCollapseIconsProps />
  </tbody>
</table>

<BrowserWindow url='/vertical-menu/nav-collapse-icons'>
  <Provider>
    <NavCollapseIcons />
  </Provider>
</BrowserWindow>

### Source Code

:::warning
The `onClick={() => console.log('clicked')}` prop written below in the `NavCollapseIcons` component is for demo purposes only. You should write the following instead in order to update the layout according to the collapsed state.

```diff
+ import useVerticalNav from '@menu/hooks/useVerticalNav'
+ import { useSettings } from '@core/hooks/useSettings'

+ const { isCollapsed } = useVerticalNav()
+ const { updateSettings } = useSettings()

- <NavCollapseIcons onClick={() => console.log('clicked')} />
+ <NavCollapseIcons onClick={() => updateSettings({ layout: !isCollapsed ? 'collapsed' : 'vertical' })} />
```
:::

<TsToJsCodeSnippet>{updatedCode}</TsToJsCodeSnippet>
