---
hide_table_of_contents: true
---

import BrowserWindow from "@docComponents/browser-window";
import Provider from "@docComponents/Providers";
import OnClick from "@docPages/menu-examples/vertical-menu/submenu/OnClick";
import TsToJsCodeSnippet from "@docComponents/ts-js-code";
import SourceCode from "!!raw-loader!@docPages/menu-examples/vertical-menu/submenu/OnClick";

# On Click

export const updatedCode = SourceCode.replace(
  /customStyles=\{\{([\s\S]*?)\}\}/,
  ""
)
  .replace("flex min-bs-full bs-dvh", "flex")
  .replace(/backgroundColor='.*'/, "")
  .replace(/\s*\/\/\sStyle.*\s*import menuItemStyles.*/, "")
  .replace(/menuItemStyles={.*}/, "");

The `onClick` prop allows you to define a custom function that will be called when the submenu is clicked. It provides a way to handle the click event and perform specific actions or logic in response to the click.

### Props

```ts noCopy
onClick?: (event: React.MouseEvent<HTMLElement>) => void
```

### Example

<BrowserWindow url="/vertical-menu/submenu/on-click">
  <Provider>
    <OnClick />
  </Provider>
</BrowserWindow>

### Source Code

<TsToJsCodeSnippet>{updatedCode}</TsToJsCodeSnippet>
