---
hide_table_of_contents: true
---

import BrowserWindow from '@docComponents/browser-window'
import Provider from '@docComponents/Providers'
import DefaultOpen from '@docPages/menu-examples/vertical-menu/submenu/DefaultOpen'
import TsToJsCodeSnippet from '@docComponents/ts-js-code'
import SourceCode from '!!raw-loader!@docPages/menu-examples/vertical-menu/submenu/DefaultOpen'

# Default Open

export const updatedCode = SourceCode.replace(/customStyles=\{\{([\s\S]*?)\}\}/, '').replace(/backgroundColor='.*'/, '').replace(/\s*\/\/\sStyle.*\s*import menuItemStyles.*/, '').replace(/menuItemStyles={.*}/, '')

The `defaultOpen` prop is used to specify whether a submenu should be open by default when the page loads. By setting the `defaultOpen` prop to true, you can indicate that a specific submenu should be initially open.

### Props

```ts noCopy
defaultOpen?: boolean
```

```ts noCopy title='Default Value'
defaultOpen={false}
```

### Example

<BrowserWindow url='/vertical-menu/submenu/default-open'>
  <Provider>
    <DefaultOpen />
  </Provider>
</BrowserWindow>

### Source Code

<TsToJsCodeSnippet>{updatedCode}</TsToJsCodeSnippet>
