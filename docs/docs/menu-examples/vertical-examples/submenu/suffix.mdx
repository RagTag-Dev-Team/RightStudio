---
hide_table_of_contents: true
---

import BrowserWindow from '@docComponents/browser-window'
import Provider from '@docComponents/Providers'
import Suffix from '@docPages/menu-examples/vertical-menu/submenu/Suffix'
import TsToJsCodeSnippet from '@docComponents/ts-js-code'
import SourceCode from '!!raw-loader!@docPages/menu-examples/vertical-menu/submenu/Suffix'

# Suffix

export const updatedCode = SourceCode.replace(/customStyles=\{\{([\s\S]*?)\}\}/, '').replace(/backgroundColor='.*'/, '').replace(/\s*\/\/\sStyle.*\s*import menuItemStyles.*/, '').replace(/menuItemStyles={.*}/, '')

The `suffix` prop allows you to add additional content or elements after the label of a submenu.

### Props

```ts noCopy
suffix?: ReactNode | ChipProps
```

### Example

<BrowserWindow url='/vertical-menu/submenu/suffix'>
  <Provider>
    <Suffix />
  </Provider>
</BrowserWindow>

### Source Code

<TsToJsCodeSnippet>{updatedCode}</TsToJsCodeSnippet>
