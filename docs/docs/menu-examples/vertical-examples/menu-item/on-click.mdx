---
hide_table_of_contents: true
---

import BrowserWindow from '@docComponents/browser-window'
import Provider from '@docComponents/Providers'
import OnClick from '@docPages/menu-examples/vertical-menu/menu-item/OnClick'
import TsToJsCodeSnippet from '@docComponents/ts-js-code'
import SourceCode from '!!raw-loader!@docPages/menu-examples/vertical-menu/menu-item/OnClick'

# On Click

The `onClick` prop allows you to define a custom function that will be called when the menu item is clicked. It provides a way to handle the click event and perform specific actions or logic in response to the click.

### Props

```ts noCopy
onClick?: (event: React.MouseEvent<HTMLElement>) => void
```

### Example

<BrowserWindow url='/vertical-menu/menu-item/on-click'>
  <Provider>
    <OnClick />
  </Provider>
</BrowserWindow>

### Source Code

<TsToJsCodeSnippet>{SourceCode}</TsToJsCodeSnippet>
