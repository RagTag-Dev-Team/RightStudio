---
hide_table_of_contents: true
---

import BrowserWindow from "@docComponents/browser-window";
import Provider from "@docComponents/Providers";
import Icon from "@docPages/menu-examples/vertical-menu/menu-item/Icon";
import TsToJsCodeSnippet from "@docComponents/ts-js-code";
import SourceCode from "!!raw-loader!@docPages/menu-examples/vertical-menu/menu-item/Icon";

# Icon

export const updatedCode = SourceCode.replace(
  /customStyles=\{\{([\s\S]*?)\}\}/,
  ""
)
  .replace(/backgroundColor='.*'/, "")
  .replace(/\s*\/\/\sStyle.*\s*import menuItemStyles.*/, "")
  .replace(/menuItemStyles={.*}/, "");

The `icon` prop allows you to add an icon to the menu item. The icon prop accepts a React element, which can be any valid JSX or component that represents the desired icon. This gives you the flexibility to use popular icon libraries, custom SVG icons, or any other icon implementation you prefer.

### Props

```ts noCopy
icon?: ReactElement
```

### Example

<BrowserWindow url="/vertical-menu/menu-item/icon">
  <Provider>
    <Icon />
  </Provider>
</BrowserWindow>

### Source Code

<TsToJsCodeSnippet>{updatedCode}</TsToJsCodeSnippet>
