---
hide_table_of_contents: true
---

import BrowserWindow from '@docComponents/browser-window'
import Provider from '@docComponents/Providers'
import Breakpoint from '@docPages/menu-examples/vertical-menu/vertical-nav/Breakpoint'
import TsToJsCodeSnippet from '@docComponents/ts-js-code'
import SourceCode from '!!raw-loader!@docPages/menu-examples/vertical-menu/vertical-nav/Breakpoint'

# Breakpoint

The `breakpoint` prop is used to determine when the navigation sidebar should trigger responsive behavior. The responsive behavior typically involves hiding the navigation sidebar and showing a hamburger menu button instead. When the hamburger menu button is clicked, the navigation sidebar is shown as an overlay. The default value for the `breakpoint` prop is `lg`, which means that the navigation sidebar will be hidden when the screen size is less than 1200px. The `breakpoint` prop can be set to `always` to always show the hamburger menu button, regardless of the screen size.

### Props

```ts noCopy
breakpoint? : 'xs' | 'sm' | 'md' | 'lg' | 'xl' | 'xxl' | 'always'
```

```ts noCopy title='Default Value'
breakpoint='lg'
```

| Value  | Description                                                                        |
| ------ | ---------------------------------------------------------------------------------- |
| xs     | < 480px                                                                            |
| sm     | < 600px                                                                            |
| md     | < 900px                                                                            |
| lg     | < 1200px                                                                           |
| xl     | < 1536px                                                                           |
| xxl    | < 1920px                                                                           |
| always | Vertical navigation is always hidden and the hamburger menu button is always shown |

### Example

<BrowserWindow>
  <Provider>
    <Breakpoint />
  </Provider>
</BrowserWindow>

### Source Code

<TsToJsCodeSnippet>{SourceCode}</TsToJsCodeSnippet>
