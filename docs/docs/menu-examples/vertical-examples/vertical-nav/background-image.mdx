---
hide_table_of_contents: true
---

import BrowserWindow from "@docComponents/browser-window";
import Provider from "@docComponents/Providers";
import BackgroundImage from "@docPages/menu-examples/vertical-menu/vertical-nav/BackgroundImage";
import TsToJsCodeSnippet from "@docComponents/ts-js-code";
import SourceCode from "!!raw-loader!@docPages/menu-examples/vertical-menu/vertical-nav/BackgroundImage";

# Background Image

export const updatedCode = SourceCode.replace(
  /customStyles=\{\{([\s\S]*?)\}\}/,
  ""
)
  .replace(/\s*\/\/\sStyle.*\s*import menuItemStyles.*/, "")
  .replace(/menuItemStyles={.*}/, "");

The `backgroundImage` prop is used to set the background image of the navigation sidebar. To make the background image visible, you also need to provide the `backgroundColor` prop. The `backgroundImage` prop accepts a string value that can be a URL or a relative path to the image.

### Props

```ts noCopy
backgroundImage?: string
```

### Example

<BrowserWindow url="/vertical-menu/vertical-nav/background-image">
  <Provider>
    <BackgroundImage />
  </Provider>
</BrowserWindow>

### Source Code

<TsToJsCodeSnippet>{updatedCode}</TsToJsCodeSnippet>
