---
hide_table_of_contents: true
---

import BrowserWindow from "@docComponents/browser-window";
import Provider from "@docComponents/Providers";
import Basic from "@docPages/menu-examples/vertical-menu/vertical-nav/Basic";
import TsToJsCodeSnippet from "@docComponents/ts-js-code";
import SourceCode from "!!raw-loader!@docPages/menu-examples/vertical-menu/vertical-nav/Basic";

# Basic

Here we have used static menu example to render the vertical menu. You may refer to the [Dynamic Menu](/docs/menu-examples/vertical-examples/menu-render/dynamic-menu) or [Using API](/docs/menu-examples/vertical-examples/menu-render/menu-with-api) examples for more information on the dynamic menu rendering.

export const updatedCode = SourceCode.replace(
  /customStyles=\{\{([\s\S]*?)\}\}/,
  ""
)
  .replace(/backgroundColor='.*'/, "")
  .replace(/\s*\/\/\sStyle.*\s*import menuItemStyles.*/, "")
  .replace(/menuItemStyles={.*}/, "");

<BrowserWindow url="/vertical-menu/vertical-nav/basic">
  <Provider>
    <Basic />
  </Provider>
</BrowserWindow>

### Source Code

<TsToJsCodeSnippet>{updatedCode}</TsToJsCodeSnippet>
