---
hide_table_of_contents: true
---

import BrowserWindow from '@docComponents/browser-window'
import Provider from '@docComponents/Providers'
import CollapsedWidth from '@docPages/menu-examples/vertical-menu/vertical-nav/CollapsedWidth'
import TsToJsCodeSnippet from '@docComponents/ts-js-code'
import SourceCode from '!!raw-loader!@docPages/menu-examples/vertical-menu/vertical-nav/CollapsedWidth'

# Collapsed Width

The `collapsedWidth` prop is used to set the width of the navigation sidebar when it is in the collapsed state. You can set it to a number value, which represents the width in pixels.

### Props

```ts noCopy
collapsedWidth?: number
```

```ts noCopy title='Default Value'
collapsedWidth={80}
```

### Example

<BrowserWindow>
  <Provider>
    <CollapsedWidth />
  </Provider>
</BrowserWindow>

### Source Code

<TsToJsCodeSnippet>{SourceCode}</TsToJsCodeSnippet>
