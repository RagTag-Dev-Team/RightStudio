---
hide_table_of_contents: true
---

import BrowserWindow from '@docComponents/browser-window'
import Provider from '@docComponents/Providers'
import TransitionDuration from '@docPages/menu-examples/vertical-menu/vertical-nav/TransitionDuration'
import TsToJsCodeSnippet from '@docComponents/ts-js-code'
import SourceCode from '!!raw-loader!@docPages/menu-examples/vertical-menu/vertical-nav/TransitionDuration'

# Transition Duration

export const updatedCode = SourceCode.replace(/customStyles=\{\{([\s\S]*?)\}\}/, '').replace('flex min-bs-full bs-dvh', 'flex').replace(/backgroundColor='.*'/, '').replace(/\s*\/\/\sStyle.*\s*import menuItemStyles.*/, '').replace(/menuItemStyles={.*}/, '')

The `transitionDuration` prop allows you to set the duration of the animation when the navigation sidebar is being collapsed, expanded or toggled. The value is in milliseconds (ms).

### Props

```ts noCopy
transitionDuration?: number
```

```ts noCopy title='Default Props'
transitionDuration = 300 // milliseconds
```

### Example

<BrowserWindow url='/vertical-menu/vertical-nav/transition-duration'>
  <Provider>
    <TransitionDuration />
  </Provider>
</BrowserWindow>

### Source Code

<TsToJsCodeSnippet>{updatedCode}</TsToJsCodeSnippet>
