---
hide_table_of_contents: true
---

import BrowserWindow from '@docComponents/browser-window'
import Provider from '@docComponents/Providers'
import BackdropColor from '@docPages/menu-examples/vertical-menu/vertical-nav/BackdropColor'
import TsToJsCodeSnippet from '@docComponents/ts-js-code'
import SourceCode from '!!raw-loader!@docPages/menu-examples/vertical-menu/vertical-nav/BackdropColor'

# Backdrop Color

export const updatedCode = SourceCode.replace(/\s*\/\/.*\s*\/\/.*\s*customStyles=\{\{([\s\S]*?)\}\}\s*\/\/.*/, '')

The backdrop color is the background color of the overlay that appears behind the navigation sidebar when it is open. The backdrop color can be changed by setting the `backdropColor` prop to any valid CSS value for background-color.

### Props

```ts noCopy
backdropColor?: string
```

```ts noCopy title='Default Value'
backdropColor='rgba(0, 0, 0, 0.3)'
```

### Examples

<BrowserWindow url='/vertical-menu/vertical-nav/backdrop-color'>
  <Provider>
    <BackdropColor />
  </Provider>
</BrowserWindow>

### Source Code

<TsToJsCodeSnippet>{updatedCode}</TsToJsCodeSnippet>
