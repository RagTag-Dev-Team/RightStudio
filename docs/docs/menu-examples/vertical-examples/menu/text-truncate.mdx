---
hide_table_of_contents: true
---

import TsToJsCodeSnippet from '@docComponents//ts-js-code'
import IframeWindow from '@docComponents//browser-window/IframeWindow.tsx'
import SourceCode from '!!raw-loader!@/app/[lang]/menu-examples/vertical-menu/menu/text-truncate/page'

# Text Truncate

The `textTruncate` functionality is a feature within a menu item, submenu, or menu section that allows for the truncation of text that exceeds the available space. The `textTruncate` prop is used to truncate the text. By default the text is truncated with an ellipsis. `textTruncate` prop provides the flexibility to disable the truncation by setting it to false.

### Props

```ts
textTruncat?: boolean
```

```ts title='Default Value'
textTruncate={true}
```

### Example

<IframeWindow url='http://localhost:3000/en/menu-examples/vertical-menu/menu/text-truncate' />

### Source Code

<TsToJsCodeSnippet>{SourceCode}</TsToJsCodeSnippet>
