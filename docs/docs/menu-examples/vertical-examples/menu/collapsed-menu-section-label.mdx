---
hide_table_of_contents: true
---

import BrowserWindow from "@docComponents/browser-window";
import Provider from "@docComponents/Providers";
import CollapsedMenuSectionLabel from "@docPages/menu-examples/vertical-menu/menu/CollapsedMenuSectionLabel";
import TsToJsCodeSnippet from "@docComponents/ts-js-code";
import SourceCode from "!!raw-loader!@docPages/menu-examples/vertical-menu/menu/CollapsedMenuSectionLabel";

# Collapsed Menu Section Label

export const updatedCode = SourceCode.replace(
  /customStyles=\{\{([\s\S]*?)\}\}/,
  ""
)
  .replace(/backgroundColor='.*'/, "")
  .replace(/\s*\/\/\sStyle.*\s*import menuItemStyles.*/, "")
  .replace(/menuItemStyles={.*}/, "");

When the menu is collapsed, the `collapsedMenuSectionLabel` prop can be used to display a custom label for each menu section.

### Props

```ts noCopy
collapsedMenuSectionLabel?: ReactNode;
```

### Example

<BrowserWindow url="/vertical-menu/menu/collapsed-menu-section-label">
  <Provider>
    <CollapsedMenuSectionLabel />
  </Provider>
</BrowserWindow>

### Source Code

<TsToJsCodeSnippet>{updatedCode}</TsToJsCodeSnippet>
