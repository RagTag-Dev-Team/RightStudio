---
hide_table_of_contents: true
---

import BrowserWindow from '@docComponents/browser-window'
import Provider from '@docComponents/Providers'
import PopoutCollapsed from '@docPages/menu-examples/vertical-menu/menu/PopoutCollapsed'
import TsToJsCodeSnippet from '@docComponents/ts-js-code'
import SourceCode from '!!raw-loader!@docPages/menu-examples/vertical-menu/menu/PopoutCollapsed'

# Popout When Collapsed

export const updatedCode = SourceCode.replace(/customStyles=\{\{([\s\S]*?)\}\}/, '').replace(/backgroundColor='.*'/, '').replace(/\s*\/\/\sStyle.*\s*import menuItemStyles.*/, '').replace(/menuItemStyles={.*}/, '')

:::danger Warning
Please note that the `defaultCollapsed` prop is meant for internal use only. It is not recommended to use this prop in your application because the layout configuration will always override the menu configuration. If you want to `collapse` the menu, please set the `layout` prop to `collapsed` in the `src/config/themeConfig.ts` file.
:::

When the navigation sidebar is collapsed, the `popoutCollapsed` prop allows submenus to pop out and be displayed on hover or click. This is useful when the menu is collapsed and you want to display the submenu items. If you want to toggle the submenu items on click (default is hover), please refer to [this](/docs/menu-examples/vertical-examples/menu/trigger-popout) example.

### Props

```ts noCopy
popoutWhenCollapsed?: boolean
```

```ts noCopy title='Default Value'
defaultCollapsed={false}
```

### Example

<BrowserWindow url='/vertical-menu/menu/popout-collapsed'>
  <Provider>
    <PopoutCollapsed />
  </Provider>
</BrowserWindow>

### Source Code

<TsToJsCodeSnippet>{updatedCode}</TsToJsCodeSnippet>
