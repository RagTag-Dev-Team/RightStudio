---
hide_table_of_contents: true
---

import BrowserWindow from "@docComponents/browser-window";
import Provider from "@docComponents/Providers";
import Toggle from "@docPages/menu-examples/vertical-menu/menu-types/Toggle";
import TsToJsCodeSnippet from "@docComponents/ts-js-code";
import SourceCode from "!!raw-loader!@docPages/menu-examples/vertical-menu/menu-types/Toggle";

# Toggle

export const updatedCode = SourceCode.replace(
  /customStyles=\{\{([\s\S]*?)\}\}/,
  ""
)
  .replace("flex min-bs-full bs-dvh", "flex")
  .replace(/backgroundColor='.*'/, "")
  .replace(/\s*\/\/\sStyle.*\s*import menuItemStyles.*/, "")
  .replace(/menuItemStyles={.*}/, "");

The toggle menu type refers to a vertical menu that can be toggled open and closed. It provides a way for the user to control the visibility of the navigation sidebar by clicking on a toggle button or icon.

### Example

<BrowserWindow url="/vertical-menu/menu-types/toggle">
  <Provider>
    <Toggle />
  </Provider>
</BrowserWindow>

### Source Code

<TsToJsCodeSnippet>{updatedCode}</TsToJsCodeSnippet>
