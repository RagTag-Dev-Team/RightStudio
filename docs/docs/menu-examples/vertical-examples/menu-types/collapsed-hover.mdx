---
hide_table_of_contents: true
---

import BrowserWindow from '@docComponents/browser-window'
import Provider from '@docComponents/Providers'
import CollapsedHover from '@docPages/menu-examples/vertical-menu/menu-types/CollapsedHover'
import TsToJsCodeSnippet from '@docComponents/ts-js-code'
import SourceCode from '!!raw-loader!@docPages/menu-examples/vertical-menu/menu-types/CollapsedHover'

# Collapsed + Hover

export const updatedCode = SourceCode.replace(/customStyles=\{\{([\s\S]*?)\}\}/, '').replace(/backgroundColor='.*'/, '').replace(/\s*\/\/\sStyle.*\s*import menuItemStyles.*/, '').replace(/menuItemStyles={.*}/, '')

:::danger Warning
Please note that the `defaultCollapsed` prop is meant for internal use only. It is recommended not to use this prop in your project because the settings configuration will always override the `defaultCollapsed` prop. If you want to `collapse` the menu, set the value of `layout` to `collapsed` in the `src/config/themeConfig.ts` file.
:::

The `defaultCollapsed` prop is used to set the menu to be collapsed by default, and it will expand on hover.

### Props

```ts noCopy
defaultCollapsed?: boolean
```

```ts noCopy title='Default Value'
defaultCollapsed={false}
```

### Example

<BrowserWindow url='/vertical-menu/menu-types/collapsed-hover'>
  <Provider>
    <CollapsedHover />
  </Provider>
</BrowserWindow>

### Source Code

<TsToJsCodeSnippet>{updatedCode}</TsToJsCodeSnippet>
