---
hide_table_of_contents: true
title: Vertical Navbar
---

import BrowserWindow from '@docComponents/browser-window'
import Provider from '@docComponents/Providers'
import VerticalNavbar from '@docPages/menu-examples/horizontal-menu/menu-position/VerticalNavbar'
import TsToJsCodeSnippet from '@docComponents/ts-js-code'
import SourceCode from '!!raw-loader!@docPages/menu-examples/horizontal-menu/menu-position/VerticalNavbar'

export const updatedCode = SourceCode.replace(/\s*\/\/.*\s*\/\/.*\s*([\s\S]*?)\s*\/\/.*/, '').replace(/import menuItemStyles.*\n/, '').replace(/import verticalMenuItemStyles.*\n/, '').replace(/menuItemStyles={.*}/, '').replace(/verticalMenuProps={.*}/, '')

# Horizontal Menu In Vertical Navbar

The following example demonstrates the rendering of a horizontal menu within a vertical navbar. This is just an example, showcasing how a horizontal menu can be rendered in any desired location within a template. This example is commonly utilized for presenting a horizontal menu in vertical navbar to users.

### Example

<BrowserWindow url='/horizontal-menu/menu-position/vertical-navbar'>
  <Provider>
    <VerticalNavbar />
  </Provider>
</BrowserWindow>

### Source Code

<TsToJsCodeSnippet>{updatedCode}</TsToJsCodeSnippet>
