---
hide_table_of_contents: true
---

import BrowserWindow from "@docComponents/browser-window";
import Provider from "@docComponents/Providers";
import WithParams from "@docPages/menu-examples/horizontal-menu/menu/popout-menu-offset/WithParams";
import WithoutParams from "@docPages/menu-examples/horizontal-menu/menu/popout-menu-offset/WithoutParams";
import TsToJsCodeSnippet from "@docComponents/ts-js-code";
import SourceCodeWithParams from "!!raw-loader!@docPages/menu-examples/horizontal-menu/menu/popout-menu-offset/WithParams";
import SourceCodeWithoutParams from "!!raw-loader!@docPages/menu-examples/horizontal-menu/menu/popout-menu-offset/WithoutParams";

# Popout Menu Offset

The `popoutMenuOffset` prop used to adjust the distance or position at which the pop-out menu is displayed or positioned in relation to its trigger or parent element.

### Props

```ts noCopy
popoutMenuOffset?: {
  mainAxis?: number | ((params: { level?: number }) => number)
  alignmentAxis?: number | ((params: { level?: number }) => number)
}
```

The `popoutMenuOffset` prop accepts an object with two optional properties:

| Property        | Description                                                                      |
| --------------- | -------------------------------------------------------------------------------- |
| `mainAxis`      | This property controls the offset distance along the main axis of the menu.      |
| `alignmentAxis` | This property controls the offset distance along the alignment axis of the menu. |

## With Parameter

The `popoutMenuOffset` prop accepts a function that receives an object with the following properties:

| Property | Description                                                                      |
| -------- | -------------------------------------------------------------------------------- |
| `level`  | The level of the menu. The root menu has a level of `0`, and so on and so forth. |

### Example

export const updatedSourceCodeWithParams = SourceCodeWithParams.replace(
  /import menuItemStyles.*\n/,
  ""
).replace(/menuItemStyles={.*}/, "");

<BrowserWindow url="/horizontal-menu/menu/popout-menu-offset/with-params">
  <Provider>
    <WithParams />
  </Provider>
</BrowserWindow>

### Source Code

<TsToJsCodeSnippet>{updatedSourceCodeWithParams}</TsToJsCodeSnippet>

## Without Parameter

The `popoutMenuOffset` prop accepts a number that will be used as the offset distance along the main axis of the menu.

### Example

export const updatedSourceCodeWithoutParams = SourceCodeWithoutParams.replace(
  /import menuItemStyles.*\n/,
  ""
).replace(/menuItemStyles={.*}/, "");

<BrowserWindow url="/horizontal-menu/menu/popout-menu-offset/without-params">
  <Provider>
    <WithoutParams />
  </Provider>
</BrowserWindow>

### Source Code

<TsToJsCodeSnippet>{updatedSourceCodeWithoutParams}</TsToJsCodeSnippet>
