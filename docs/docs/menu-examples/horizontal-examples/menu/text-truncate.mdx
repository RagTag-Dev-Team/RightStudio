---
hide_table_of_contents: true
---

import BrowserWindow from '@docComponents/browser-window'
import Provider from '@docComponents/Providers'
import TextTruncate from '@docPages/menu-examples/horizontal-menu/menu/TextTruncate'
import TsToJsCodeSnippet from '@docComponents/ts-js-code'
import SourceCode from '!!raw-loader!@docPages/menu-examples/horizontal-menu/menu/TextTruncate'

# Text Truncate

export const updatedCode = SourceCode.replace(/customStyles=\{\{([\s\S]*?)\}\}/, '').replace(/backgroundColor='.*'/, '').replace(/import menuItemStyles.*\n/, '').replace(/\s*\/\/\sMUI.*\s*import\s{ deepmerge.*/,'').replace(/menuItemStyles={([\s\S]*?)\)\}/, '')

The `textTruncate` functionality is a feature within a menu item and submenu that allows for the truncation of text that exceeds the available space. The `textTruncate` prop is used to truncate the text. By default the text is truncated with an ellipsis. `textTruncate` prop provides the flexibility to disable the truncation by setting it to false.

### Props

```ts
textTruncat?: boolean
```

```ts title='Default Value'
textTruncate={true}
```

### Example

<BrowserWindow url='/horizontal-menu/menu/text-truncate'>
  <Provider>
    <TextTruncate />
  </Provider>
</BrowserWindow>

### Source Code

<TsToJsCodeSnippet>{updatedCode}</TsToJsCodeSnippet>
