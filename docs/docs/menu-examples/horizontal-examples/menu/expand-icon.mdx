---
hide_table_of_contents: true
---

import BrowserWindow from '@docComponents/browser-window'
import Provider from '@docComponents/Providers'
import WithParams from '@docPages/menu-examples/horizontal-menu/menu/expand-icon/WithParams'
import WithoutParams from '@docPages/menu-examples/horizontal-menu/menu/expand-icon/WithoutParams'
import TsToJsCodeSnippet from '@docComponents/ts-js-code'
import SourceCodeWithParams from '!!raw-loader!@docPages/menu-examples/horizontal-menu/menu/expand-icon/WithParams'
import SourceCodeWithoutParams from '!!raw-loader!@docPages/menu-examples/horizontal-menu/menu/expand-icon/WithoutParams'

# Expand Menu Icon

The `renderExpandIcon` prop allows you to customize the icon used for expanding the menu. The `renderExpandIcon` prop takes a `RenderExpandIconParams` object as a parameter. This function allows you to fully customize the expand menu icon based on different conditions or states.

### Props

The `RenderExpandIconParams` object passed to the renderExpandIcon function contains the following properties:

```ts noCopy
type renderExpandIcon?: (params: RenderExpandIconParams) => ReactElement

type RenderExpandIconParams = {
  open: boolean
  level: number
  active: boolean
  disabled: boolean
}
```

| Property | Description                                               |
| -------- | --------------------------------------------------------- |
| open     | Whether the menu section is open or closed.               |
| level    | Representing the depth level of the menu section.         |
| active   | Whether the menu section is currently active or selected. |
| disabled | Whether the menu item is disabled or not                  |

## With Parameter

The `renderExpandIcon` prop is a function that takes a `RenderExpandIconParams` object as a parameter.

### Example

<BrowserWindow url='/horizontal-menu/menu/expand-icon/with-params'>
  <Provider>
    <WithParams />
  </Provider>
</BrowserWindow>

### Source Code

<TsToJsCodeSnippet>{SourceCodeWithParams}</TsToJsCodeSnippet>

## Without Parameter

The `renderExpandIcon` prop takes a `ReactElement`. You can also customize the expand menu icon without any parameters.

### Example

<BrowserWindow url='/horizontal-menu/menu/expand-icon/without-params'>
  <Provider>
    <WithoutParams />
  </Provider>
</BrowserWindow>

### Source Code

<TsToJsCodeSnippet>{SourceCodeWithoutParams}</TsToJsCodeSnippet>
