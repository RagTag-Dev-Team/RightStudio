---
hide_table_of_contents: true
---

import BrowserWindow from '@docComponents/browser-window'
import Provider from '@docComponents/Providers'
import Prefix from '@docPages/menu-examples/horizontal-menu/menu-item/Prefix'
import TsToJsCodeSnippet from '@docComponents/ts-js-code'
import SourceCode from '!!raw-loader!@docPages/menu-examples/horizontal-menu/menu-item/Prefix'

# Prefix

export const updatedCode = SourceCode.replace(/import menuItemStyles.*\n/, '').replace(/menuItemStyles={.*}/, '')

The `prefix` prop allows you to add additional content or elements before the label of a menu item.

### Props

```ts noCopy
prefix?: ReactNode | ChipProps
```

### Example

<BrowserWindow url='/horizontal-menu/menu-item/prefix'>
  <Provider>
    <Prefix />
  </Provider>
</BrowserWindow>

### Source Code

<TsToJsCodeSnippet>{updatedCode}</TsToJsCodeSnippet>
