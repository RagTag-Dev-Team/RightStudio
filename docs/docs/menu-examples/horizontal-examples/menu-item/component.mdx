---
hide_table_of_contents: true
---

import BrowserWindow from "@docComponents/browser-window";
import Provider from "@docComponents/Providers";
import Component from "@docPages/menu-examples/horizontal-menu/menu-item/Component";
import TsToJsCodeSnippet from "@docComponents/ts-js-code";
import SourceCode from "!!raw-loader!@docPages/menu-examples/horizontal-menu/menu-item/Component";

# Component

export const updatedCode = SourceCode.replace(
  /import menuItemStyles.*\n/,
  ""
).replace(/menuItemStyles={.*}/, "");

The `component` prop allows you to change the wrapper component of a menu item. The `component` prop accepts either a string or a ReactElement. If you pass a string, it will be used as the HTML tag for the wrapping component. If you pass a ReactElement, it will be rendered as the wrapping component itself; for example, `<Link>` component from a routing library.

### Props

```ts noCopy
component?: string | ReactElement
```

```tsx noCopy title='Default Value'
component = <a></a>;
```

### Example

<BrowserWindow url="/horizontal-menu/menu-item/component">
  <Provider>
    <Component />
  </Provider>
</BrowserWindow>

### Source Code

<TsToJsCodeSnippet>{updatedCode}</TsToJsCodeSnippet>
