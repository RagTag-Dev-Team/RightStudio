---
hide_table_of_contents: true
---

import BrowserWindow from "@docComponents/browser-window";
import Provider from "@docComponents/Providers";
import Disabled from "@docPages/menu-examples/horizontal-menu/submenu/Disabled";
import TsToJsCodeSnippet from "@docComponents/ts-js-code";
import SourceCode from "!!raw-loader!@docPages/menu-examples/horizontal-menu/submenu/Disabled";

# Disabled

export const updatedCode = SourceCode.replace(
  /import menuItemStyles.*\n/,
  ""
).replace(/menuItemStyles={.*}/, "");

The Disabled feature allows you to disable a submenu, preventing user interaction and indicating that the item is currently inactive or unavailable. The `disabled` prop is used to control the disabled state of a menu item.

By setting the `disabled` prop to true, the corresponding submenu will be visually disabled and user interactions, such as clicks or hover effects, will be prevented.

### Props

```ts noCopy
disabled?: boolean
```

```ts noCopy title='Default Value'
disabled={false}
```

### Example

<BrowserWindow url="/horizontal-menu/submenu/disabled">
  <Provider>
    <Disabled />
  </Provider>
</BrowserWindow>

### Source Code

<TsToJsCodeSnippet>{updatedCode}</TsToJsCodeSnippet>
