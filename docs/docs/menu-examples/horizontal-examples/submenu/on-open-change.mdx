---
hide_table_of_contents: true
---

import BrowserWindow from '@docComponents/browser-window'
import Provider from '@docComponents/Providers'
import WithParams from '@docPages/menu-examples/horizontal-menu/submenu/on-open-change/WithParams'
import WithoutParams from '@docPages/menu-examples/horizontal-menu/submenu/on-open-change/WithoutParams'
import TsToJsCodeSnippet from '@docComponents/ts-js-code'
import SourceCodeWithParams from '!!raw-loader!@docPages/menu-examples/horizontal-menu/submenu/on-open-change/WithParams'
import SourceCodeWithoutParams from '!!raw-loader!@docPages/menu-examples/horizontal-menu/submenu/on-open-change/WithoutParams'

# On Open Change

The `onOpenChange` prop is used to handle the changes when a submenu is opened or closed.

### Props

```ts noCopy
onOpenChange?: (open: boolean) => void
```

## With Parameter

export const updatedSourceCodeWithParams = SourceCodeWithParams.replace(/import menuItemStyles.*\n/, '').replace(/menuItemStyles={.*}/, '')

This example demonstrates the `onOpenChange` prop with a boolean parameter. When the submenu is opened or closed, the `onOpenChange` function is called with a boolean parameter indicating the new state of the submenu.

### Example

<BrowserWindow url='/horizontal-menu/submenu/on-open-change/with-params'>
  <Provider>
    <WithParams />
  </Provider>
</BrowserWindow>

### Source Code

<TsToJsCodeSnippet>{updatedSourceCodeWithParams}</TsToJsCodeSnippet>

## Without Parameter

export const updatedSourceCodeWithoutParams = SourceCodeWithoutParams.replace(/import menuItemStyles.*\n/, '').replace(/menuItemStyles={.*}/, '')

This example illustrates the `onOpenChange` prop without any parameters.

### Example

<BrowserWindow url='/horizontal-menu/submenu/on-open-change/without-params'>
  <Provider>
    <WithoutParams />
  </Provider>
</BrowserWindow>

### Source Code

<TsToJsCodeSnippet>{updatedSourceCodeWithoutParams}</TsToJsCodeSnippet>
