---
hide_table_of_contents: true
---

import BrowserWindow from "@docComponents/browser-window";
import Provider from "@docComponents/Providers";
import RootStyles from "@docPages/menu-examples/horizontal-menu/submenu/RootStyles";
import TsToJsCodeSnippet from "@docComponents/ts-js-code";
import SourceCode from "!!raw-loader!@docPages/menu-examples/horizontal-menu/submenu/RootStyles";

# Root Styles

export const updatedCode = SourceCode.replace(
  /import menuItemStyles.*\n/,
  ""
).replace(/menuItemStyles={.*}/, "");

The Root Style feature provides the flexibility to apply custom styling to the submenu. With the `rootStyles` prop, you can pass a set of CSS properties to customize the appearance of the submenu according to your design preferences. The `rootStyles` prop accepts a CSSObject from the [Emotion](https://emotion.sh/docs/introduction) library.

### Props

```ts noCopy
type RootStylesType = {
  rootStyles?: CSSObject;
};
```

### Example

<BrowserWindow url="/horizontal-menu/submenu/root-styles">
  <Provider>
    <RootStyles />
  </Provider>
</BrowserWindow>

### Source Code

<TsToJsCodeSnippet>{updatedCode}</TsToJsCodeSnippet>
