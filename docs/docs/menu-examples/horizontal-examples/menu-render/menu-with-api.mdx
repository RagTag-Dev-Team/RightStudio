---
hide_table_of_contents: true
---

import BrowserWindow from "@docComponents/browser-window";
import Provider from "@docComponents/Providers";
import MenuWithApi from "@docPages/menu-examples/horizontal-menu/menu-render/MenuWithApi";
import TsToJsCodeSnippet from "@docComponents/ts-js-code";
import SourceCode from "!!raw-loader!@docPages/menu-examples/horizontal-menu/menu-render/MenuWithApi";

# Menu With API

export const updatedCode = SourceCode.replace(
  "@docComponents/GenerateMenu",
  "@compnoents/GenerateMenu"
)
  .replace("@site/src/types/menuTypes", "@/types/menuTypes")
  .replace(/customStyles=\{\{([\s\S]*?)\}\}/, "")
  .replace(/\s*\/\/\sMUI.*\s*import\s{ deepmerge.*/, "")
  .replace(/import menuItemStyles.*\n/, "")
  .replace(/menuItemStyles={([\s\S]*?)\}\)\}/, "");

This example demonstrates how to create a dynamic menu by fetching menu data from an API endpoint. This approach allows you to retrieve menu items from a server or database, making it easy to manage and update the menu content.

### Example

<BrowserWindow url="/horizontal-menu/menu-render/menu-with-api">
  <Provider>
    <MenuWithApi />
  </Provider>
</BrowserWindow>

### Source Code

<TsToJsCodeSnippet>{updatedCode}</TsToJsCodeSnippet>
