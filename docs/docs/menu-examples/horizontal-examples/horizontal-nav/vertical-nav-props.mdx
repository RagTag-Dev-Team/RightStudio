import BrowserWindow from '@docComponents/browser-window'
import Provider from '@docComponents/Providers'
import Width from '@docPages/menu-examples/horizontal-menu/horizontal-nav/vertical-nav-props/Width'
import BackgroundColor from '@docPages/menu-examples/horizontal-menu/horizontal-nav/vertical-nav-props/BackgroundColor'
import BackgroundImage from '@docPages/menu-examples/horizontal-menu/horizontal-nav/vertical-nav-props/BackgroundImage'
import CustomStyles from '@docPages/menu-examples/horizontal-menu/horizontal-nav/vertical-nav-props/CustomStyles'
import TsToJsCodeSnippet from '@docComponents/ts-js-code'
import SourceCodeWidth from "!!raw-loader!@docPages/menu-examples/horizontal-menu/horizontal-nav/vertical-nav-props/Width";
import SourceCodeBackgroundColor from "!!raw-loader!@docPages/menu-examples/horizontal-menu/horizontal-nav/vertical-nav-props/BackgroundColor";
import SourceCodeBackgroundImage from "!!raw-loader!@docPages/menu-examples/horizontal-menu/horizontal-nav/vertical-nav-props/BackgroundImage";
import SourceCodeStyle from "!!raw-loader!@docPages/menu-examples/horizontal-menu/horizontal-nav/vertical-nav-props/CustomStyles";

# Vertical Nav Props

export const updatedSourceCodeWidth = SourceCodeWidth.replace(/\s*\/\/.*\s*\/\/.*\s*([\s\S]*?)\s*\/\/.*/, '').replace(/import menuItemStyles.*\n/, '').replace(/import verticalMenuItemStyles.*\n/, '').replace(/menuItemStyles={.*}/, '').replace(/verticalMenuProps={.*}/, '').replace(/(verticalNavProps={{)\s*/, '$1')

export const updatedSourceCodeBackgroundColor = SourceCodeBackgroundColor.replace(/\s*\/\/.*\s*\/\/.*\s*([\s\S]*?)\s*\/\/.*/, '').replace(/import menuItemStyles.*\n/, '').replace(/import verticalMenuItemStyles.*\n/, '').replace(/menuItemStyles={.*}/, '').replace(/verticalMenuProps={.*}/, '').replace(/(verticalNavProps={{)\s*/, '$1')

export const updatedSourceCodeBackgroundImage = SourceCodeBackgroundImage.replace(/\s*\/\/.*\s*\/\/.*\s*([\s\S]*?)\s*\/\/.*/, '').replace(/import menuItemStyles.*\n/, '').replace(/import verticalMenuItemStyles.*\n/, '').replace(/menuItemStyles={.*}/, '').replace(/verticalMenuProps={.*}/, '')

export const updatedSourceCodeStyle = SourceCodeStyle.replace(/,\s*\/\/.*\s*\/\/.*\s*([\s\S]*?)\s*\/\/.*/, '').replace(/backgroundColor:\s*'.*'/, '').replace(/import menuItemStyles.*\n/, '').replace(/import verticalMenuItemStyles.*\n/, '').replace(/menuItemStyles={.*}/, '').replace(/verticalMenuProps={.*}/, '')

:::warning
Please note that the `VerticalNavContent` used in this example is only for documentation purposes. If you want to use it in your own component, please import it from `src/components/layout/horizontal/VerticalNavContent.tsx` and use it accordingly.
:::

The `verticalNavProps` prop is used to change the appearance of the vertical navigation menu when breakpoint is reached.

## Width

The `width` is used to change the width of the vertical navigation menu when breakpoint is reached.

### Example

<BrowserWindow url='/horizontal-menu/horizontal-nav/vertical-nav-props/width'>
  <Provider>
    <Width />
  </Provider>
</BrowserWindow>

### Source Code

<TsToJsCodeSnippet>{updatedSourceCodeWidth}</TsToJsCodeSnippet>

## Background Color

The `backgroundColor` is used to change the background color of the vertical navigation menu when breakpoint is reached.

### Example

<BrowserWindow url='/horizontal-menu/horizontal-nav/vertical-nav-props/background-color'>
  <Provider>
    <BackgroundColor />
  </Provider>
</BrowserWindow>

### Source Code

<TsToJsCodeSnippet>{updatedSourceCodeBackgroundColor}</TsToJsCodeSnippet>

## Background Image

The `backgroundImage` is used to change the background image of the vertical navigation menu when breakpoint is reached.

### Example

<BrowserWindow url='/horizontal-menu/horizontal-nav/vertical-nav-props/background-image'>
  <Provider>
    <BackgroundImage />
  </Provider>
</BrowserWindow>

### Source Code

<TsToJsCodeSnippet>{updatedSourceCodeBackgroundImage}</TsToJsCodeSnippet>

## Custom Styles

The Custom Style feature provides the flexibility to apply custom styling to the vertical navigation menu when breakpoint is reached. With the `customStyles` prop, you can pass a set of CSS properties to customize the appearance of the navigation menu according to your design preferences. The `customStyles` prop accepts a CSSObject from the [Emotion](https://emotion.sh/docs/introduction) library.

### Example

<BrowserWindow url='/horizontal-menu/horizontal-nav/vertical-nav-props/custom-styles'>
  <Provider>
    <CustomStyles />
  </Provider>
</BrowserWindow>

### Source Code

<TsToJsCodeSnippet>{updatedSourceCodeStyle}</TsToJsCodeSnippet>
