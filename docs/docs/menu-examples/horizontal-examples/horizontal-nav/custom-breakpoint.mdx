---
hide_table_of_contents: true
---

import BrowserWindow from '@docComponents/browser-window'
import Provider from '@docComponents/Providers'
import CustomBreakpoint from '@docPages/menu-examples/horizontal-menu/horizontal-nav/CustomBreakpoint'
import TsToJsCodeSnippet from '@docComponents/ts-js-code'
import SourceCode from '!!raw-loader!@docPages/menu-examples/horizontal-menu/horizontal-nav/CustomBreakpoint'

# Custom Breakpoint

export const updatedCode = SourceCode.replace(/\s*\/\/.*\s*\/\/.*\s*verticalNavProps=\{\{([\s\S]*?)\}\}\s*.*\s*.*\/\/.*/, '').replace(/import menuItemStyles.*\n/, '').replace(/import verticalMenuItemStyles.*\n/, '').replace(/menuItemStyles={.*}/, '').replace(/verticalMenuProps={.*}/, '')

:::warning
Please note that the `VerticalNavContent` used in this example is only for documentation purposes. If you want to use it in your own component, please import it from `src/components/layout/horizontal/VerticalNavContent.tsx` and use it accordingly.
:::

The `customBreakpoint` prop lets you choose your own size for when the navigation menu changes. Normally, this happens on a large screen (1200px), but you can set it to a different size if you want. This way, you don't have to use the default breakpoint values listed [here](/docs/menu-examples/horizontal-examples/horizontal-nav/breakpoint).

### Props

```ts noCopy
customBreakpoint?: string
```

### Example

<BrowserWindow url='/horizontal-menu/horizontal-nav/custom-breakpoint'>
  <Provider>
    <CustomBreakpoint />
  </Provider>
</BrowserWindow>

### Source Code

<TsToJsCodeSnippet>{updatedCode}</TsToJsCodeSnippet>
