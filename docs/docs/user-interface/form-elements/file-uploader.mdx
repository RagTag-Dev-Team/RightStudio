---
hide_table_of_contents: true
title: ''
sidebar_label: File Uploader
---

import Grid from '@mui/material/Grid'

import CardSnippet from '@docComponents/card-snippet/index'
import AppReactDropzone from '@docComponents/styled-components/AppReactDropzone'
import FileUploaderSingle from '@docPages/form-elements/file-uploader/FileUploaderSingle'
import SourceCodeFileUploaderSingle from '!!raw-loader!@docPages/form-elements/file-uploader/FileUploaderSingle'
import FileUploaderMultiple from '@docPages/form-elements/file-uploader/FileUploaderMultiple'
import SourceCodeFileUploaderMultiple from '!!raw-loader!@docPages/form-elements/file-uploader/FileUploaderMultiple'
import FileUploaderRestrictions from '@docPages/form-elements/file-uploader/FileUploaderRestrictions'
import SourceCodeFileUploaderRestrictions from '!!raw-loader!@docPages/form-elements/file-uploader/FileUploaderRestrictions'

<h1 className='!m-0'>React Dropzone</h1>

<div className='template-bg-color' />

<code>react-dropzone</code> is a third-party library. Please refer to its [official documentation](https://react-dropzone.js.org/) for more details.

:::warning
It is essential to ensure that when incorporating the <code>react-dropzone</code> component into your project, it is correctly wrapped using the <code>AppReactDropzone</code> component. In scenarios where only a single instance of <code>react-dropzone</code> is utilized, it should be individually enclosed within the <code>AppReactDropzone</code> component. Conversely, if multiple instances of <code>react-dropzone</code> are used in one page, it becomes necessary to wrap the entire page with the <code>AppReactDropzone</code> component to ensure optimal styles.
:::

<AppReactDropzone>
  <Grid container spacing={6}>
    <Grid item xs={12}>
      <CardSnippet
        title='Upload Multiple Files'
        code={SourceCodeFileUploaderMultiple}
      >
        <FileUploaderMultiple />
      </CardSnippet>
    </Grid>
    <Grid item xs={12}>
      <CardSnippet
        title='Upload Single Files'
        code={SourceCodeFileUploaderSingle}
      >
        <FileUploaderSingle />
      </CardSnippet>
    </Grid>
    <Grid item xs={12}>
      <CardSnippet
        title='Upload Files with Restrictions'
        code={SourceCodeFileUploaderRestrictions}
      >
        <FileUploaderRestrictions />
      </CardSnippet>
    </Grid>
  </Grid>
</AppReactDropzone>