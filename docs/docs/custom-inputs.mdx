# Custom Inputs

import Tabs from '@theme/Tabs'
import TabItem from '@theme/TabItem'

import TsToJsCodeSnippet from '@docComponents/ts-js-code'
import CustomHorizontalRadio from '!!raw-loader!@docPages/form-elements/custom-inputs/CustomHorizontalRadio'
import CustomHorizontalCheckbox from '!!raw-loader!@docPages/form-elements/custom-inputs/CustomHorizontalCheckbox'
import CustomVerticalRadioIcon from '!!raw-loader!@docPages/form-elements/custom-inputs/CustomVerticalRadioIcon'
import CustomVerticalCheckboxIcon from '!!raw-loader!@docPages/form-elements/custom-inputs/CustomVerticalCheckboxIcon'
import CustomRadioWithImage from '!!raw-loader!@docPages/form-elements/custom-inputs/CustomRadioWithImage'
import CustomCheckboxWithImage from '!!raw-loader!@docPages/form-elements/custom-inputs/CustomCheckboxWithImage'

## Overview

We have created custom radio and checkbox components to save your time and make it easier for you. You can find detailed explanations of the Radio component in the [MUI Radio Docs](https://mui.com/material-ui/react-radio-button/) and the Checkbox component in the [MUI Checkbox Docs](https://mui.com/material-ui/react-checkbox/).

We have already added these components in the `MUI` components. More information on how to create `Themeable component` of MUI can be found [here](https://mui.com/material-ui/guides/themeable-component/#introduction).

## Horizontal Inputs

You can use horizontal inputs as radio or checkbox. You can show title, meta and/or content in the input. 

### Props

```tsx
type CustomInputHorizontalData = {
  value: string // Identify a particular input
  content?: ReactNode // Content of the input
  isSelected?: boolean // If true, the input will be selected at the initial render
} & (
  | {
      meta: ReactNode // Add content to the right side of the title. (To add meta in your input, you need to add the title property as well)
      title: ReactNode // Title for the radio component
    }
  | {
      meta?: never
      title?: never
    }
  | {
      title: ReactNode
      meta?: never
    }
)
type CustomInputHorizontalProps = {
  name: string // Name attribute of the input
  color?: 'primary' | 'secondary' | 'error' | 'warning' | 'info' | 'success' // Color of the selected input
  gridProps?: GridProps // Add props of the MUI's Grid component
  data: CustomInputHorizontalData // Object to create input
} & (
  | {
      type: 'checkbox' // Type of the input
      selected: string[] // Array of selected values
      handleChange: (value: string) => void // Run a function when an option is changed
    }
  | {
      type: 'radio' // Type of the input
      selected: string // Value of the selected radio
      handleChange: (value: string | ChangeEvent<HTMLInputElement>) => void // Run a function when an option is changed
    }
)
```

### Horizontal Radio Example

<TsToJsCodeSnippet>{CustomHorizontalRadio}</TsToJsCodeSnippet>

### Horizontal Checkbox Example

<TsToJsCodeSnippet>{CustomHorizontalCheckbox}</TsToJsCodeSnippet>

## Vertical Inputs

You can use vertical inputs as radio or checkbox. You can show title, content and/or asset in the input. In asset you can add an image, an icon or an SVG.

:::caution Warning
If you are passing an SVG as an asset, you need to pass the SVG as a React component.
:::

### Props

```tsx
type CustomInputVerticalData = {
  value: string // Identify a particular input
  title?: ReactNode // Title for the input
  content?: ReactNode // Content of the input
  isSelected?: boolean // If true, the input will be selected at the initial render
  asset?: ReactNode // Render an asset in the input like an image or an icon or svg
}
type CustomInputVerticalProps = {
  name: string // Name attribute of the input
  color?: 'primary' | 'secondary' | 'error' | 'warning' | 'info' | 'success' // Color of the selected input
  gridProps?: GridProps // Add props of the MUI's Grid component
  data: CustomInputVerticalData // Object to create input
} & ({
  type: 'checkbox' // Type of the input
  selected: string[] // Array of selected values
  handleChange: (value: string) => void // Run a function when an option is changed
} | {
  type: 'radio' // Type of the input
  selected: string // Value of the selected radio
  handleChange: (value: string | ChangeEvent<HTMLInputElement>) => void // Run a function when an option is changed
})
```

### Vertical Radio Example

<TsToJsCodeSnippet>{CustomVerticalRadioIcon}</TsToJsCodeSnippet>

### Vertical Checkbox Example

<TsToJsCodeSnippet>{CustomVerticalCheckboxIcon}</TsToJsCodeSnippet>

## Inputs With image

You can use inputs with image as radio or checkbox. You need to show an image in the input.

### Props

```tsx
type CustomInputImgData = {
  alt?: string // Alternate text for the image
  value: string // Identify a particular input
  img: ReactNode // Image for the input
  isSelected?: boolean // If true, the input will be selected at the initial render
}
type CustomInputImgProps = {
  name: string // Name attribute of the input
  color?: 'primary' | 'secondary' | 'error' | 'warning' | 'info' | 'success' // Color of the selected input
  gridProps?: GridProps // Add props of the MUI's Grid component
  data: CustomInputImgData // Object to create input
} & ({
  type: 'checkbox' // Type of the input
  selected: string[] // Array of selected values
  handleChange: (value: string) => void // Run a function when an option is changed
} | {
  type: 'radio' // Type of the input
  selected: string // Value of the selected radio
  handleChange: (value: string | ChangeEvent<HTMLInputElement>) => void // Run a function when an option is changed
})
```

### Radio With image Example

<TsToJsCodeSnippet>{CustomRadioWithImage}</TsToJsCodeSnippet>

### Checkbox With image Example

<TsToJsCodeSnippet>{CustomCheckboxWithImage}</TsToJsCodeSnippet>

## How to override components

To override or customized the components, follow these steps:

Let us say you want to customize the `CustomInputHorizontal` component.(you can customize other custom input components as well by following the same steps).

1. You need to create a new file (you can name the file whichever you want; let us say that you have named `src/components/custom-inputs/UserHorizontal.tsx` file). Copy the code from `src/@core/components/custom-inputs/horizontal.tsx` file, paste it into the new file. Also copy the related types from `src/@core/components/custom-inputs/types.ts` file and paste it into the `src/components/custom-inputs/types.ts` file.
2. Now, you can make changes as per your requirements in the `UserHorizontal.tsx` file.
3. We have already added the `CustomInputHorizontal` component in the `MUI` components. You may refer to the `src/compponents/theme/type.ts` file on how to add a custom component in the `MUI` components. 

That's it! Now you can import the `UserHorizontal` component in your project and use it.

## How to override styles 

To override or customized the styles, follow these steps:

Let us say you want to customize the `CustomInputHorizontal` component.(you can customize other custom input components as well by following the same steps).

You can override the styles in both the `src/components/theme/userTheme.ts` or `src/components/theme/mergedTheme.ts` file.

You may refer to the following code snippet:

<Tabs>
<TabItem value="MergedTheme.ts">

```tsx title='src/components/theme/MergedTheme.ts'
const MergedTheme = () => {
  const userTheme = {
    components: {
      MuiCustomInputHorizontal: {
        styleOverrides: {
          title : {
            color: '#fff',
            fontSize: '1.25rem',
          }
        }
      }
    }
  } as Theme

return deepmerge(coreTheme(), userTheme)
}
```

</TabItem>
<TabItem value="userTheme.ts">

```tsx title='src/components/theme/userTheme.ts'
const userTheme = (): Theme => {
  return {
    components: {
      MuiCustomInputHorizontal: {
        styleOverrides: {
          title : {
            color: '#fff',
            fontSize: '1.25rem',
          }
        }
      }
    }
  } as Theme
}
```

</TabItem>
</Tabs>

You have to add `mergedTheme` or `userTheme` in `index.tsx` file. You may refer to [this](/docs/guide/development/theming/overview#common-customization-steps) on how to implement it.

That's it !
